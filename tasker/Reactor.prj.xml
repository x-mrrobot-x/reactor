<TaskerData sr="" dvi="1" tv="6.7.6-beta">
	<dmetric>1080.0,2307.0</dmetric>
	<Profile sr="prof2020" ve="2">
		<cdate>1785950121852</cdate>
		<edate>1786617360188</edate>
		<flags>40</flags>
		<id>2020</id>
		<mid0>2013</mid0>
		<nme>RC - START EVENT MONITOR</nme>
		<Event sr="con0" ve="2">
			<code>307</code>
			<pri>0</pri>
		</Event>
	</Profile>
	<Project sr="proj0" ve="2">
		<cdate>1745400000000</cdate>
		<id>3fc9c99c-2059-4be3-b354-4c4df60830e9</id>
		<name>REACTOR</name>
		<pids>2020</pids>
		<scenes>REACTOR</scenes>
		<tids>2018,2016,2014,2013</tids>
		<tsort>Alpha</tsort>
	</Project>
	<Scene sr="sceneREACTOR">
		<cdate>1785556154439</cdate>
		<edate>1785558346136</edate>
		<heightLand>-1</heightLand>
		<heightPort>-1</heightPort>
		<lj></lj>
		<nme>REACTOR</nme>
		<widthLand>-1</widthLand>
		<widthPort>-1</widthPort>
	</Scene>
	<Task sr="task2013">
		<cdate>1745400000000</cdate>
		<edate>1786617360188</edate>
		<id>2013</id>
		<pri>6</pri>
		<Action sr="act0" ve="7">
			<code>547</code>
			<Str sr="arg0" ve="3">%action</Str>
			<Str sr="arg1" ve="3">toggle_monitor</Str>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
			<Int sr="arg5" val="3"/>
			<Int sr="arg6" val="1"/>
		</Action>
		<Action sr="act1" ve="7">
			<code>130</code>
			<Str sr="arg0" ve="3">RC 02 - MONITOR CONTROL</Str>
			<Int sr="arg1" val="10"/>
			<Int sr="arg10" val="0"/>
			<Str sr="arg2" ve="3"/>
			<Str sr="arg3" ve="3"/>
			<Str sr="arg4" ve="3"/>
			<Int sr="arg5" val="0"/>
			<Int sr="arg6" val="1"/>
			<Str sr="arg7" ve="3">%action</Str>
			<Int sr="arg8" val="0"/>
			<Int sr="arg9" val="0"/>
		</Action>
	</Task>
	<Task sr="task2014">
		<cdate>1745400000000</cdate>
		<edate>1786541870162</edate>
		<id>2014</id>
		<nme>RC 03 - COMMAND EXECUTOR</nme>
		<pri>1000</pri>
		<Action sr="act0" ve="7">
			<code>37</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%par1</lhs>
					<op>2</op>
					<rhs>flash</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act1" ve="7">
			<code>511</code>
			<Int sr="arg0" val="2"/>
			<Str sr="arg1" ve="3"/>
		</Action>
		<Action sr="act10" ve="7">
			<code>20</code>
			<App sr="arg0">
				<appClass>com.foxdebug.acode.MainActivity</appClass>
				<appPkg>com.foxdebug.acode</appPkg>
				<label>Acode</label>
			</App>
			<Str sr="arg1" ve="3"/>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
		</Action>
		<Action sr="act11" ve="7">
			<code>43</code>
		</Action>
		<Action sr="act12" ve="7">
			<code>548</code>
			<Str sr="arg0" ve="3">Comando desconhecido: %par1</Str>
			<Int sr="arg1" val="0"/>
			<Str sr="arg10" ve="3">0</Str>
			<Int sr="arg11" val="1"/>
			<Int sr="arg12" val="0"/>
			<Str sr="arg13" ve="3"/>
			<Int sr="arg14" val="0"/>
			<Str sr="arg15" ve="3"/>
			<Int sr="arg2" val="1"/>
			<Str sr="arg3" ve="3"/>
			<Str sr="arg4" ve="3"/>
			<Str sr="arg5" ve="3"/>
			<Str sr="arg6" ve="3"/>
			<Str sr="arg7" ve="3"/>
			<Str sr="arg8" ve="3"/>
			<Int sr="arg9" val="1"/>
		</Action>
		<Action sr="act13" ve="7">
			<code>38</code>
		</Action>
		<Action sr="act14" ve="7">
			<code>548</code>
			<Str sr="arg0" ve="3">Comando executado: %par1</Str>
			<Int sr="arg1" val="0"/>
			<Str sr="arg10" ve="3"/>
			<Int sr="arg11" val="1"/>
			<Int sr="arg12" val="0"/>
			<Str sr="arg13" ve="3"/>
			<Int sr="arg14" val="0"/>
			<Str sr="arg15" ve="3"/>
			<Int sr="arg2" val="1"/>
			<Str sr="arg3" ve="3"/>
			<Str sr="arg4" ve="3"/>
			<Str sr="arg5" ve="3"/>
			<Str sr="arg6" ve="3"/>
			<Str sr="arg7" ve="3"/>
			<Str sr="arg8" ve="3"/>
			<Int sr="arg9" val="1"/>
		</Action>
		<Action sr="act2" ve="7">
			<code>43</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%par1</lhs>
					<op>2</op>
					<rhs>kill</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act3" ve="7">
			<code>335</code>
			<Bundle sr="arg0">
				<Vals sr="val">
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES>&lt;StringArray sr=""&gt;&lt;_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES0&gt;%app_icon
Icon
Icon for the app&lt;/_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES0&gt;&lt;_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES1&gt;%app_name
Name
Name of the App&lt;/_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES1&gt;&lt;_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES2&gt;%app_package
Package
Unique package name for the app&lt;/_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES2&gt;&lt;/StringArray&gt;</net.dinglisch.android.tasker.RELEVANT_VARIABLES>
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>[Ljava.lang.String;</net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>
				</Vals>
			</Bundle>
			<Str sr="arg1" ve="3"/>
			<Str sr="arg2" ve="3"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
		</Action>
		<Action sr="act4" ve="7">
			<code>18</code>
			<App sr="arg0">
				<appClass></appClass>
				<appPkg>%app_package</appPkg>
				<label>%app_package</label>
			</App>
			<Int sr="arg1" val="0"/>
		</Action>
		<Action sr="act5" ve="7">
			<code>43</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%par1</lhs>
					<op>2</op>
					<rhs>wifi</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act6" ve="7">
			<code>341</code>
			<Int sr="arg0" val="0"/>
			<Str sr="arg1" ve="3"/>
			<Str sr="arg2" ve="3">%type</Str>
		</Action>
		<Action sr="act7" ve="7">
			<code>375</code>
			<Bundle sr="arg0">
				<Vals sr="val">
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES>&lt;StringArray sr=""&gt;&lt;_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES0&gt;%aw_output
Output
Output resulting from the command. May be empty.&lt;/_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES0&gt;&lt;/StringArray&gt;</net.dinglisch.android.tasker.RELEVANT_VARIABLES>
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>[Ljava.lang.String;</net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>
				</Vals>
			</Bundle>
			<Str sr="arg1" ve="3">svc wifi enable</Str>
			<Str sr="arg2" ve="3"/>
			<Str sr="arg3" ve="3"/>
			<Int sr="arg4" val="10"/>
			<Int sr="arg5" val="0"/>
			<Str sr="arg6" ve="3"/>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%type</lhs>
					<op>3</op>
					<rhs>wifi</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act8" ve="7">
			<code>375</code>
			<Bundle sr="arg0">
				<Vals sr="val">
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES>&lt;StringArray sr=""&gt;&lt;_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES0&gt;%aw_output
Output
Output resulting from the command. May be empty.&lt;/_array_net.dinglisch.android.tasker.RELEVANT_VARIABLES0&gt;&lt;/StringArray&gt;</net.dinglisch.android.tasker.RELEVANT_VARIABLES>
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>[Ljava.lang.String;</net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>
				</Vals>
			</Bundle>
			<Str sr="arg1" ve="3">svc wifi disable</Str>
			<Str sr="arg2" ve="3"/>
			<Str sr="arg3" ve="3"/>
			<Int sr="arg4" val="10"/>
			<Int sr="arg5" val="0"/>
			<Str sr="arg6" ve="3"/>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%type</lhs>
					<op>2</op>
					<rhs>wifi</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act9" ve="7">
			<code>43</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%par1</lhs>
					<op>2</op>
					<rhs>acode</rhs>
				</Condition>
			</ConditionList>
		</Action>
	</Task>
	<Task sr="task2016">
		<cdate>1785555608857</cdate>
		<edate>1786176031965</edate>
		<id>2016</id>
		<nme>RC 01 - MAIN</nme>
		<pri>100</pri>
		<Action sr="act0" ve="7">
			<code>479</code>
			<Bundle sr="arg0">
				<Vals sr="val">
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES>&lt;StringArray sr=""/&gt;</net.dinglisch.android.tasker.RELEVANT_VARIABLES>
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>[Ljava.lang.String;</net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>
				</Vals>
			</Bundle>
			<Str sr="arg1" ve="3">REACTOR</Str>
			<Str sr="arg10" ve="3"/>
			<Str sr="arg11" ve="3"/>
			<Int sr="arg12" val="0"/>
			<Int sr="arg13" val="0"/>
			<Int sr="arg14" val="0"/>
			<Str sr="arg15" ve="3"/>
			<Int sr="arg16" val="1"/>
			<Str sr="arg2" ve="3"/>
			<Str sr="arg3" ve="3">Fullscreen</Str>
			<Str sr="arg4" ve="3"/>
			<Str sr="arg5" ve="3"/>
			<Str sr="arg6" ve="3"/>
			<Str sr="arg7" ve="3"/>
			<Int sr="arg8" val="1"/>
			<Str sr="arg9" ve="3"/>
		</Action>
	</Task>
	<Task sr="task2018">
		<cdate>1745400000000</cdate>
		<edate>1786617370595</edate>
		<id>2018</id>
		<nme>RC 02 - MONITOR CONTROL</nme>
		<pri>100</pri>
		<rty>1</rty>
		<Action sr="act0" ve="7">
			<code>37</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%action</lhs>
					<op>2</op>
					<rhs>get_status</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act1" ve="7">
			<code>547</code>
			<Str sr="arg0" ve="3">%java_code</Str>
			<Str sr="arg1" ve="3">import org.json.JSONObject;
import java.io.File;
import java.io.FileReader;

CONFIG_PATH = "/storage/emulated/0/Tasker/Reactor/reactor.json";

rawCfg = tasker.getJavaVariable("reactorConfig");
if (rawCfg != null &amp;&amp; rawCfg != void) {
    cfgRoot = (JSONObject) rawCfg;
} else {
    file = new File(CONFIG_PATH);
    if (file.exists()) {
        reader = null;
        try {
            reader = new FileReader(file);
            buf = new char[(int) file.length()];
            reader.read(buf);
            cfgRoot = new JSONObject(new String(buf));
            tasker.setJavaVariable("reactorConfig", cfgRoot);
        } finally {
            if (reader != null) reader.close();
        }
    } else {
        cfgRoot = null;
    }
}

monStatus = tasker.getJavaVariable("stopReactorMonitor");
monitorActive = (monStatus != null &amp;&amp; monStatus != void);

result = new JSONObject();
result.put("ok", true);
result.put("monitorActive", monitorActive);
result.put("config", cfgRoot != null ? cfgRoot : JSONObject.NULL);
return result.toString();</Str>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
			<Int sr="arg5" val="3"/>
			<Int sr="arg6" val="0"/>
		</Action>
		<Action sr="act10" ve="7">
			<code>547</code>
			<Str sr="arg0" ve="3">%jc_result</Str>
			<Str sr="arg1" ve="3">{"ok":false,"error":"ação desconhecida"}</Str>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
			<Int sr="arg5" val="3"/>
			<Int sr="arg6" val="0"/>
		</Action>
		<Action sr="act11" ve="7">
			<code>38</code>
		</Action>
		<Action sr="act12" ve="7">
			<code>547</code>
			<Str sr="arg0" ve="3">%result</Str>
			<Str sr="arg1" ve="3">{ "id": "%call_id", "payload": %jc_result }</Str>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
			<Int sr="arg5" val="3"/>
			<Int sr="arg6" val="0"/>
		</Action>
		<Action sr="act13" ve="7">
			<code>596</code>
			<Str sr="arg0" ve="3">%result</Str>
			<Int sr="arg1" val="24"/>
			<Str sr="arg2" ve="3">%result</Str>
			<Int sr="arg3" val="0"/>
		</Action>
		<Action sr="act14" ve="7">
			<code>485</code>
			<Bundle sr="arg0">
				<Vals sr="val">
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES>&lt;StringArray sr=""/&gt;</net.dinglisch.android.tasker.RELEVANT_VARIABLES>
					<net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>[Ljava.lang.String;</net.dinglisch.android.tasker.RELEVANT_VARIABLES-type>
				</Vals>
			</Bundle>
			<Str sr="arg1" ve="3">REACTOR</Str>
			<Str sr="arg2" ve="3">MONITOR</Str>
			<Str sr="arg3" ve="3">load_url</Str>
			<Str sr="arg4" ve="3">url</Str>
			<Str sr="arg5" ve="3">javascript:onTaskResponse(`%result`)</Str>
			<Str sr="arg6" ve="3"/>
		</Action>
		<Action sr="act2" ve="7">
			<code>43</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%action</lhs>
					<op>2</op>
					<rhs>toggle_monitor</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act3" ve="7">
			<code>547</code>
			<Str sr="arg0" ve="3">%java_code</Str>
			<Str sr="arg1" ve="3">import io.reactivex.subjects.CompletableSubject;
import io.reactivex.disposables.Disposable;
import io.reactivex.functions.Action;
import io.reactivex.functions.Consumer;
import io.reactivex.functions.Predicate;
import java.util.concurrent.TimeUnit;
import org.json.JSONObject;
import org.json.JSONArray;
import android.view.accessibility.AccessibilityEvent;
import android.view.accessibility.AccessibilityNodeInfo;
import android.accessibilityservice.AccessibilityService;
import android.accessibilityservice.GestureDescription;
import android.graphics.Path;
import android.os.Bundle;
import android.os.Build;
import android.os.Vibrator;
import android.os.VibrationEffect;
import android.content.Intent;
import android.content.Context;
import android.content.ClipData;
import android.content.ClipDescription;
import android.content.ClipboardManager;
import android.net.Uri;
import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;
import java.net.URLEncoder;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.MediaType;
import okhttp3.RequestBody;

try {

rawCfg = tasker.getJavaVariable("reactorConfig");
if (rawCfg == null || rawCfg == void) {
    throw new JavaCodeException("Configuração não carregada. Abra o painel primeiro.");
}
cfgRoot = (JSONObject) rawCfg;

// Constantes
REACTOR_AI_ERROR_NOTIF_ID = 990001;
APP_EVENT_DEDUPE_MS = 1200;
MAX_REGEX_INPUT_LEN = 500;
CLIPBOARD_SELF_WRITE_SUPPRESS_MS = 800;

setFieldTextAndMark(node, text) {
    if (node == null) return false;
    args = new Bundle();
    args.putCharSequence(AccessibilityNodeInfo.ACTION_ARGUMENT_SET_TEXT_CHARSEQUENCE, text);
    node.performAction(AccessibilityNodeInfo.ACTION_SET_TEXT, args);
    tasker.setJavaVariable("reactorLastFieldWriteTs", System.currentTimeMillis());
    return true;
}

launchExternalIntent(intent, node) {
    cleared = setFieldTextAndMark(node, "");
    intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
    context.startActivity(intent);
    return cleared;
}

vibrateTriggerDetected() {
    vib = (Vibrator) context.getSystemService(Context.VIBRATOR_SERVICE);
    if (vib == null || !vib.hasVibrator()) return;
    if (Build.VERSION.SDK_INT &gt;= 26) {
        vib.vibrate(VibrationEffect.createOneShot(15, VibrationEffect.DEFAULT_AMPLITUDE));
    } else {
        vib.vibrate(15);
    }
}

showLoadingIndicator(node, textBeforeTrigger) {
    setFieldTextAndMark(node, textBeforeTrigger + "\u23F3");
}

clearLoadingIndicator(node) {
    setFieldTextAndMark(node, "");
}

vibrateError() {
    vib = (Vibrator) context.getSystemService(Context.VIBRATOR_SERVICE);
    if (vib == null || !vib.hasVibrator()) return;
    pattern = new long[]{0, 40, 80, 40};
    if (Build.VERSION.SDK_INT &gt;= 26) {
        vib.vibrate(VibrationEffect.createWaveform(pattern, -1));
    } else {
        vib.vibrate(pattern, -1);
    }
}

showSystemNotification(id, title, body) {
    nm = (NotificationManager) context.getSystemService(Context.NOTIFICATION_SERVICE);
    channelId = "reactor_channel";
    nb = new Notification.Builder(context);
    nb.setContentTitle(title);
    nb.setContentText(body);
    nb.setSmallIcon(android.R.drawable.ic_dialog_info);
    nb.setOnlyAlertOnce(true);
    if (Build.VERSION.SDK_INT &gt;= 26) nb.setChannelId(channelId);
    nm.notify(id, nb.build());
}

buildResult(active, message) {
    result = new JSONObject();
    result.put("ok", true);
    result.put("monitorActive", active);
    result.put("message", message);
    return result.toString();
}

applyTemplate(template, ctx) {
    if (template == null) return "";
    result = template;
    it = ctx.keySet().iterator();
    while (it.hasNext()) {
        key = (String) it.next();
        val = ctx.get(key);
        result = result.replace("{" + key + "}", val != null ? (String) val : "");
    }
    return result;
}

collectReferencedVariables(rule) {
    refs = new HashSet();
    actions = rule.optJSONArray("actions") != null ? rule.getJSONArray("actions") : new JSONArray();
    varPattern = Pattern.compile("\\{(\\w+)\\}");
    for (i = 0; i &lt; actions.length(); i++) {
        action = actions.getJSONObject(i);
        actType = action.getString("type");
        config = action.has("config") ? action.getJSONObject("config") : new JSONObject();
        if (actType.equals("ai") || actType.equals("translate")) {
            iv = config.optString("inputVariable", "");
            if (iv.length() &gt; 0) refs.add(iv);
        } else {
            keys = config.keys();
            while (keys.hasNext()) {
                fieldKey = (String) keys.next();
                fieldVal = config.opt(fieldKey);
                if (fieldVal instanceof String) {
                    m = varPattern.matcher((String) fieldVal);
                    while (m.find()) refs.add(m.group(1));
                }
            }
        }
    }
    return refs;
}

resolveAppName(pkg) {
    if (pkg == null || pkg.length() == 0) return "";
    appNameCache = tasker.getJavaVariable("reactorAppNameCache");
    if (appNameCache == null || appNameCache == void) {
        appNameCache = new HashMap();
        tasker.setJavaVariable("reactorAppNameCache", appNameCache);
    }
    if (appNameCache.containsKey(pkg)) return (String) appNameCache.get(pkg);
    name = pkg;
    try {
        pm = context.getPackageManager();
        appInfo = pm.getApplicationInfo(pkg, 0);
        name = pm.getApplicationLabel(appInfo).toString();
    } catch (Exception e) {
        name = pkg;
    }
    appNameCache.put(pkg, name);
    return name;
}

getCachedPattern(cv) {
    regexCache = tasker.getJavaVariable("reactorRegexCache");
    if (regexCache == null || regexCache == void) {
        regexCache = new HashMap();
        tasker.setJavaVariable("reactorRegexCache", regexCache);
    }
    pattern = (Pattern) regexCache.get(cv);
    if (pattern == null) {
        pattern = Pattern.compile(cv, Pattern.CASE_INSENSITIVE);
        regexCache.put(cv, pattern);
    }
    return pattern;
}

executeAction(actions, index, ctx, node, cfgRootArg, dispatchState) {
    if (index &gt;= actions.length()) return;
    actionObj = actions.getJSONObject(index);
    actionType = actionObj.getString("type");
    actionConfig = actionObj.has("config") ? actionObj.getJSONObject("config") : new JSONObject();

    if (actionType.equals("text_replacer") || actionType.equals("ai") || actionType.equals("translate")) {
        dispatchState.put("hasRewritingAction", Boolean.TRUE);
    }

    if (actionType.equals("ai") || actionType.equals("translate")) {
        final HashMap finalContext = ctx;
        final JSONArray finalActions = actions;
        final int finalIndex = index;
        final AccessibilityNodeInfo finalNode = node;
        final JSONObject finalCfgRoot = cfgRootArg;
        final JSONObject finalActionConfig = actionConfig;
        final String finalActionType = actionType;
        final HashMap finalDispatchState = dispatchState;

        inputKey = finalActionConfig.optString("inputVariable", "");
        final String inputText = finalContext.containsKey(inputKey) ? (String) finalContext.get(inputKey) : "";

        new Thread(new Runnable() {
            run() {
                resultText = null;
                failureReason = null;
                try {
                    client = new OkHttpClient.Builder()
                        .connectTimeout(8, TimeUnit.SECONDS)
                        .readTimeout(8, TimeUnit.SECONDS)
                        .writeTimeout(8, TimeUnit.SECONDS)
                        .build();

                    if (finalActionType.equals("translate")) {
                        lang = finalActionConfig.optString("language", "en");
                        text = inputText.trim();
                        if (text.length() &gt; 0) {
                            url = "https://translate.googleapis.com/translate_a/single?client=gtx&amp;sl=auto&amp;dt=t&amp;q="
                                + URLEncoder.encode(text, "UTF-8") + "&amp;tl=" + lang;
                            req = new Request.Builder().url(url).build();
                            resp = client.newCall(req).execute();
                            if (resp.isSuccessful()) {
                                root = new JSONArray(resp.body().string());
                                sentences = root.getJSONArray(0);
                                sb2 = new StringBuilder();
                                for (si = 0; si &lt; sentences.length(); si++) sb2.append(sentences.getJSONArray(si).getString(0));
                                resultText = sb2.toString();
                            } else {
                                failureReason = "Falha ao traduzir (erro do servidor).";
                                tasker.log("Reactor Tradução: resposta HTTP " + resp.code());
                            }
                            resp.close();
                        }
                        // texto vazio: nada a traduzir, não é um erro.

                    } else {
                        text = inputText.trim();
                        sysInstr = finalActionConfig.optString("systemInstructions", "");
                        gemini = finalCfgRoot.getJSONObject("settings").getJSONObject("gemini");
                        apiKey = gemini.optString("apiKey", "");
                        model = gemini.optString("model", "");

                        if (text.length() == 0) {
                            // nada a processar, não é um erro.
                        } else if (apiKey.length() == 0 || model.length() == 0) {
                            failureReason = "Chave de API do Gemini não configurada. Abra o Reactor &gt; Configurações &gt; IA · Gemini.";
                            tasker.log("Reactor IA: chave de API ou modelo não configurados em Configurações &gt; IA - Gemini.");
                        } else {
                            part = new JSONObject(); part.put("text", text);
                            parts = new JSONArray(); parts.put(part);
                            contentObj = new JSONObject(); contentObj.put("parts", parts);
                            contents = new JSONArray(); contents.put(contentObj);
                            geminiBody = new JSONObject();
                            if (sysInstr.length() &gt; 0) {
                                sp = new JSONObject(); sp.put("text", sysInstr);
                                spArr = new JSONArray(); spArr.put(sp);
                                siObj = new JSONObject(); siObj.put("parts", spArr);
                                geminiBody.put("systemInstruction", siObj);
                            }
                            geminiBody.put("contents", contents);

                            url = "https://generativelanguage.googleapis.com/v1beta/models/" + model + ":generateContent?key=" + apiKey;
                            mediaType = MediaType.parse("application/json; charset=utf-8");
                            reqBody = RequestBody.create(mediaType, geminiBody.toString());
                            req = new Request.Builder().url(url).post(reqBody).build();
                            resp = client.newCall(req).execute();
                            if (resp.isSuccessful()) {
                                root = new JSONObject(resp.body().string());
                                cand = root.getJSONArray("candidates").getJSONObject(0);
                                respParts = cand.getJSONObject("content").getJSONArray("parts");
                                sb2 = new StringBuilder();
                                for (pi = 0; pi &lt; respParts.length(); pi++) sb2.append(respParts.getJSONObject(pi).getString("text"));
                                resultText = sb2.toString().trim();
                            } else {
                                failureReason = "Falha ao processar com IA (erro do servidor Gemini).";
                                tasker.log("Reactor IA: resposta HTTP " + resp.code());
                            }
                            resp.close();
                        }
                    }
                } catch (Exception e) {
                    failureReason = "Falha ao processar com IA (erro inesperado).";
                    tasker.log("Erro Reactor (" + finalActionType + "): " + e.toString());
                }

                resultKey = finalActionType.equals("translate") ? "translate_result" : "ai_result";
                finalContext.put(resultKey, resultText != null ? resultText : "");

                outputMode = finalActionConfig.optString("outputMode", "expose_variable");
                if (outputMode.equals("replace_field")) {
                    if (resultText != null &amp;&amp; resultText.length() &gt; 0) {
                        setFieldTextAndMark(finalNode, resultText);
                    } else if (failureReason != null) {
                        // Falha real: restaura o texto original do usuário em vez de apagar o campo,
                        // e avisa via notificação + vibração já que a WebView pode não estar em primeiro plano.
                        originalInput = finalContext.containsKey("input") ? (String) finalContext.get("input") : "";
                        setFieldTextAndMark(finalNode, originalInput);
                        vibrateError();
                        showSystemNotification(REACTOR_AI_ERROR_NOTIF_ID, "Reactor", failureReason);
                    } else {
                        // Sem resultado mas também sem erro (ex: nada a processar).
                        clearLoadingIndicator(finalNode);
                    }
                }

                executeAction(finalActions, finalIndex + 1, finalContext, finalNode, finalCfgRoot, finalDispatchState);
            }
        }).start();
        return;
    }

    if (actionType.equals("text_replacer")) {
        inputVal = ctx.containsKey("input") ? (String) ctx.get("input") : "";
        replacement = applyTemplate(actionConfig.optString("text", ""), ctx);
        setFieldTextAndMark(node, inputVal + replacement);

    } else if (actionType.equals("clipboard")) {
        clipText = applyTemplate(actionConfig.optString("text", ""), ctx);
        // Marca o horário da escrita para o monitor de Clipboard ignorar o eco desta própria alteração.
        tasker.setJavaVariable("reactorLastClipboardWriteTs", System.currentTimeMillis());
        cm = (ClipboardManager) context.getSystemService(Context.CLIPBOARD_SERVICE);
        if (cm != null) {
            cm.setPrimaryClip(ClipData.newPlainText("reactor", clipText));
        }

    } else if (actionType.equals("run_task")) {
        taskName = actionConfig.optString("task", "");
        if (taskName.length() &gt; 0 &amp;&amp; taskName.equals("RC 02 - MONITOR CONTROL")) {
            tasker.log("Reactor: regra tentou chamar a própria task de controle (RC 02 - MONITOR CONTROL) via run_task — ignorado para evitar dependência circular.");
        } else if (taskName.length() &gt; 0) {
            par1 = applyTemplate(actionConfig.optString("par1", ""), ctx);
            par2 = applyTemplate(actionConfig.optString("par2", ""), ctx);
            taskVars = new HashMap();
            taskVars.put("par1", par1);
            taskVars.put("par2", par2);
            tasker.callTask(taskName, taskVars);
        }

    } else if (actionType.equals("open_app")) {
        pkgName = actionConfig.optString("package", "");
        if (pkgName.length() &gt; 0) {
            pm = context.getPackageManager();
            launchIntent = pm.getLaunchIntentForPackage(pkgName);
            if (launchIntent != null) {
                alreadyClearedField = launchExternalIntent(launchIntent, node);
                dispatchState.put("alreadyClearedField", Boolean.valueOf(alreadyClearedField));
            }
        }

    } else if (actionType.equals("open_url")) {
        urlText = applyTemplate(actionConfig.optString("url", ""), ctx);
        if (urlText.length() &gt; 0) {
            alreadyClearedField = launchExternalIntent(new Intent(Intent.ACTION_VIEW, Uri.parse(urlText)), node);
            dispatchState.put("alreadyClearedField", Boolean.valueOf(alreadyClearedField));
        }

    } else if (actionType.equals("search")) {
        engine = actionConfig.optString("engine", "google");
        queryTemplate = actionConfig.optString("query", "{input}");
        resolvedQuery = applyTemplate(queryTemplate, ctx).trim();
        query = URLEncoder.encode(resolvedQuery, "UTF-8");
        searchUrl = "https://www.google.com/search?q=" + query;
        if (engine.equals("bing")) searchUrl = "https://www.bing.com/search?q=" + query;
        else if (engine.equals("duckduckgo")) searchUrl = "https://duckduckgo.com/?q=" + query;
        else if (engine.equals("youtube")) searchUrl = "https://www.youtube.com/results?search_query=" + query;
        alreadyClearedField = launchExternalIntent(new Intent(Intent.ACTION_VIEW, Uri.parse(searchUrl)), node);
        dispatchState.put("alreadyClearedField", Boolean.valueOf(alreadyClearedField));

    } else if (actionType.equals("notification")) {
        notifTitleText = applyTemplate(actionConfig.optString("title", "Reactor"), ctx);
        notifBodyText = applyTemplate(actionConfig.optString("body", ""), ctx);
        ruleIdForNotif = ctx.containsKey("_rule_id") ? (String) ctx.get("_rule_id") : "";
        notifId = (ruleIdForNotif + "#" + index).hashCode();
        showSystemNotification(notifId, notifTitleText, notifBodyText);

    } else if (actionType.equals("click")) {
        cx = actionConfig.optDouble("x", -1);
        cy = actionConfig.optDouble("y", -1);
        svc = tasker.getAccessibilityService();
        if (svc != null &amp;&amp; cx &gt;= 0 &amp;&amp; cy &gt;= 0 &amp;&amp; Build.VERSION.SDK_INT &gt;= 24) {
            path = new Path();
            path.moveTo((float) cx, (float) cy);
            stroke = new GestureDescription.StrokeDescription(path, 0, 50);
            gb = new GestureDescription.Builder();
            gb.addStroke(stroke);
            ((AccessibilityService) svc).dispatchGesture(gb.build(), null, null);
        }
    }

    executeAction(actions, index + 1, ctx, node, cfgRootArg, dispatchState);
}

dispatchProviderEvent(provider, rawValue, eventContext, sourceNode, liveCfg) {
    if (rawValue == null || rawValue.length() == 0) return;

    providerRules = liveCfg.getJSONObject("rules_by_provider").optJSONArray(provider);
    if (providerRules == null || providerRules.length() == 0) return;

    anyRuleMatched = false;
    loadingShown = false;
    dispatchState = new HashMap();
    dispatchState.put("hasRewritingAction", Boolean.FALSE);
    dispatchState.put("alreadyClearedField", Boolean.FALSE);

    

    for (ri = 0; ri &lt; providerRules.length(); ri++) {
        rule = providerRules.getJSONObject(ri);
        if (!rule.optBoolean("enabled", false)) continue;

        eventObj = rule.getJSONObject("event");

        testValue = rawValue;
        if (provider.equals("notification")) {
            matchFieldKey = eventObj.optString("matchField", "body");
            testValue = eventContext.containsKey(matchFieldKey) ? (String) eventContext.get(matchFieldKey) : "";
            if (testValue == null) testValue = "";
        }

        conditions = rule.optJSONArray("conditions") != null ? rule.getJSONArray("conditions") : new JSONArray();
        passed = true;
        matchedEndsWithValue = null;
        for (ci = 0; ci &lt; conditions.length(); ci++) {
            cond = conditions.getJSONObject(ci);
            op = cond.getString("operator");
            cv = cond.optString("value", "");
            ok = false;
            if (op.equals("match")) {
                ok = testValue.equalsIgnoreCase(cv);
            } else if (op.equals("contains")) {
                ok = testValue.toLowerCase().contains(cv.toLowerCase());
            } else if (op.equals("starts_with")) {
                ok = testValue.toLowerCase().startsWith(cv.toLowerCase());
            } else if (op.equals("ends_with")) {
                ok = testValue.toLowerCase().endsWith(cv.toLowerCase());
            } else if (op.equals("regex")) {
                try {
                    regexPattern = getCachedPattern(cv);
                    safeTestValue = testValue.length() &gt; MAX_REGEX_INPUT_LEN
                        ? testValue.substring(0, MAX_REGEX_INPUT_LEN)
                        : testValue;
                    ok = regexPattern.matcher(safeTestValue).find();
                } catch (Exception pe) {
                    ok = false;
                }
            }
            if (ok &amp;&amp; op.equals("ends_with")) matchedEndsWithValue = cv;
            if (!ok) { passed = false; break; }
        }
        if (!passed) continue;

        anyRuleMatched = true;

        ruleContext = new HashMap(eventContext);
        ruleContext.put("_rule_id", rule.optString("id", ""));
        textBeforeTrigger = rawValue;
        triggerText = "";
        if (provider.equals("keyboard") &amp;&amp; matchedEndsWithValue != null) {
            cutIndex = rawValue.length() - matchedEndsWithValue.length();
            if (cutIndex &lt; 0) cutIndex = 0;
            textBeforeTrigger = rawValue.substring(0, cutIndex);
            triggerText = rawValue.substring(cutIndex);
        }
        if (provider.equals("keyboard")) {
            ruleContext.put("input", textBeforeTrigger);
            ruleContext.put("trigger", triggerText);
        }
        final String finalTextBeforeTrigger = textBeforeTrigger;

        referencedVars = collectReferencedVariables(rule);
        if (referencedVars.contains("app_name") &amp;&amp; ruleContext.containsKey("app_package")) {
            ruleContext.put("app_name", resolveAppName((String) ruleContext.get("app_package")));
        }

        if (provider.equals("keyboard") &amp;&amp; !loadingShown) {
            vibrateTriggerDetected();

            ruleActionsPeek = rule.optJSONArray("actions") != null ? rule.getJSONArray("actions") : new JSONArray();
            ruleHasAsyncAction = false;
            for (pk = 0; pk &lt; ruleActionsPeek.length(); pk++) {
                peekType = ruleActionsPeek.getJSONObject(pk).getString("type");
                if (peekType.equals("ai") || peekType.equals("translate")) {
                    ruleHasAsyncAction = true;
                    break;
                }
            }
            if (ruleHasAsyncAction) {
                showLoadingIndicator(sourceNode, finalTextBeforeTrigger);
            }

            loadingShown = true;
        }

        actions = rule.optJSONArray("actions") != null ? rule.getJSONArray("actions") : new JSONArray();
        executeAction(actions, 0, ruleContext, sourceNode, liveCfg, dispatchState);
    }

    if (provider.equals("keyboard") &amp;&amp; anyRuleMatched
        &amp;&amp; !((Boolean) dispatchState.get("hasRewritingAction")).booleanValue()
        &amp;&amp; !((Boolean) dispatchState.get("alreadyClearedField")).booleanValue()) {
        setFieldTextAndMark(sourceNode, "");
    }
}

sig = tasker.getJavaVariable("stopReactorMonitor");
currentlyActive = (sig != null &amp;&amp; sig != void);

if (currentlyActive) {
    s = (CompletableSubject) sig;
    s.onComplete();
    tasker.setJavaVariable("stopReactorMonitor", null);
    
    return buildResult(false, "Monitor parado");
}

if (tasker.getAccessibilityService() == null) {
    throw new JavaCodeException("Acessibilidade do Tasker está desligada.");
}

if (Build.VERSION.SDK_INT &gt;= 26) {
    nmSetup = (NotificationManager) context.getSystemService(Context.NOTIFICATION_SERVICE);
    nmSetup.createNotificationChannel(new NotificationChannel("reactor_channel", "Reactor", NotificationManager.IMPORTANCE_DEFAULT));
}

stopSignal = CompletableSubject.create();
tasker.setJavaVariable("stopReactorMonitor", stopSignal);

events = tasker.getAccessibilityEvents();

KEYBOARD_DEBOUNCE_MS = 150;

keyboardTextEvents = events.filter(new Predicate() {
    test(Object o) {
        return ((AccessibilityEvent) o).getEventType() == AccessibilityEvent.TYPE_VIEW_TEXT_CHANGED;
    }
}).debounce(KEYBOARD_DEBOUNCE_MS, TimeUnit.MILLISECONDS);

otherProviderEvents = events.filter(new Predicate() {
    test(Object o) {
        return ((AccessibilityEvent) o).getEventType() != AccessibilityEvent.TYPE_VIEW_TEXT_CHANGED;
    }
});

events = io.reactivex.Observable.merge(keyboardTextEvents, otherProviderEvents);

disposable = events.subscribe(new Consumer() {
    accept(Object eventObject) {
        try {
            event = (AccessibilityEvent) eventObject;
            type = event.getEventType();

            provider = null;
            sourceNode = null;
            rawValue = null;
            eventContext = new HashMap();

            liveCfg = (JSONObject) tasker.getJavaVariable("reactorConfig");
            if (liveCfg == null) return;
            
            liveProvidersObj = liveCfg.getJSONObject("providers");

            if (type == AccessibilityEvent.TYPE_VIEW_TEXT_CHANGED) {
                if (!liveProvidersObj.has("keyboard") || !liveProvidersObj.getJSONObject("keyboard").optBoolean("enabled", false)) return;

                lastWriteObj = tasker.getJavaVariable("reactorLastFieldWriteTs");
                if (lastWriteObj != null &amp;&amp; lastWriteObj != void) {
                    elapsedSinceWrite = System.currentTimeMillis() - ((Long) lastWriteObj).longValue();
                    if (elapsedSinceWrite &lt; 600) return;
                }

                sourceNode = event.getSource();
                if (sourceNode == null || sourceNode.getText() == null) return;
                provider = "keyboard";
                rawValue = sourceNode.getText().toString();

                pkgObj = event.getPackageName();
                eventContext.put("app_package", pkgObj != null ? pkgObj.toString() : "");

            } else if (type == AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED) {
                pkgObj = event.getPackageName();
                if (pkgObj != null) {
                    // Rastreamento leve e incondicional do app em primeiro plano — usado como
                    // melhor esforço pelo provider Clipboard para identificar a origem de uma
                    // alteração, independente do provider "app" estar habilitado.
                    tasker.setJavaVariable("reactorLastForegroundPkg", pkgObj.toString());
                    tasker.setJavaVariable("reactorLastForegroundTs", System.currentTimeMillis());
                }

                if (!liveProvidersObj.has("app") || !liveProvidersObj.getJSONObject("app").optBoolean("enabled", false)) return;
                if (pkgObj == null) return;
                rawPkg = pkgObj.toString();

                lastAppPkgObj = tasker.getJavaVariable("reactorLastAppPkg");
                lastAppTsObj = tasker.getJavaVariable("reactorLastAppTs");
                if (lastAppPkgObj != null &amp;&amp; lastAppPkgObj != void &amp;&amp; lastAppTsObj != null &amp;&amp; lastAppTsObj != void) {
                    elapsedSinceAppEvent = System.currentTimeMillis() - ((Long) lastAppTsObj).longValue();
                    if (rawPkg.equals((String) lastAppPkgObj) &amp;&amp; elapsedSinceAppEvent &lt; APP_EVENT_DEDUPE_MS) return;
                }
                tasker.setJavaVariable("reactorLastAppPkg", rawPkg);
                tasker.setJavaVariable("reactorLastAppTs", System.currentTimeMillis());

                provider = "app";
                rawValue = rawPkg;
                eventContext.put("app_package", rawValue);

            } else if (type == AccessibilityEvent.TYPE_NOTIFICATION_STATE_CHANGED) {
                if (!liveProvidersObj.has("notification") || !liveProvidersObj.getJSONObject("notification").optBoolean("enabled", false)) return;
                texts = event.getText();
                if (texts == null || texts.isEmpty()) return;

                notifTitle = texts.get(0).toString();
                sbBody = new StringBuilder();
                for (ti = 1; ti &lt; texts.size(); ti++) {
                    if (ti &gt; 1) sbBody.append(" ");
                    sbBody.append(texts.get(ti).toString());
                }
                sbFull = new StringBuilder();
                for (ti = 0; ti &lt; texts.size(); ti++) {
                    if (ti &gt; 0) sbFull.append(" ");
                    sbFull.append(texts.get(ti).toString());
                }

                pkgObj = event.getPackageName();

                provider = "notification";
                rawValue = sbFull.toString();

                eventContext.put("app_package", pkgObj != null ? pkgObj.toString() : "");
                eventContext.put("title", notifTitle);
                eventContext.put("body", sbBody.toString());
                eventContext.put("full_text", sbFull.toString());

            } else {
                return;
            }

            dispatchProviderEvent(provider, rawValue, eventContext, sourceNode, liveCfg);

        } catch (Exception e) {
            tasker.log("Erro Reactor dispatcher: " + e.toString());
        }
    }
}, new Consumer() {
    accept(Object errorObject) {
        tasker.log("Erro fatal no monitor Reactor: " + errorObject.toString());
    }
});

    tasker.setJavaVariable("reactorMonitorDisposable", disposable);

    // --- Provider Clipboard: registro orientado a evento (sem polling) ---
    clipboardManager = (ClipboardManager) context.getSystemService(Context.CLIPBOARD_SERVICE);
    if (clipboardManager != null) {
        // Salvaguarda de lifecycle: se por algum motivo sobrou um listener de uma
        // execução anterior mal finalizada, remove antes de registrar um novo.
        staleClipboardListener = tasker.getJavaVariable("reactorClipboardListener");
        if (staleClipboardListener != null &amp;&amp; staleClipboardListener != void) {
            try {
                clipboardManager.removePrimaryClipChangedListener((ClipboardManager.OnPrimaryClipChangedListener) staleClipboardListener);
            } catch (Exception ignoredStaleListenerError) {
                // Já não estava mais registrado — seguro ignorar.
            }
            tasker.setJavaVariable("reactorClipboardListener", null);
        }

        clipboardListener = new ClipboardManager.OnPrimaryClipChangedListener() {
            onPrimaryClipChanged() {
                try {
                    liveCfgClip = (JSONObject) tasker.getJavaVariable("reactorConfig");
                    if (liveCfgClip == null) return;

                    liveProvidersClip = liveCfgClip.getJSONObject("providers");
                    if (!liveProvidersClip.has("clipboard") || !liveProvidersClip.getJSONObject("clipboard").optBoolean("enabled", false)) return;

                    // Ignora o eco de alterações feitas pela própria action "Definir Clipboard".
                    lastSelfWriteObj = tasker.getJavaVariable("reactorLastClipboardWriteTs");
                    if (lastSelfWriteObj != null &amp;&amp; lastSelfWriteObj != void) {
                        elapsedSinceSelfWrite = System.currentTimeMillis() - ((Long) lastSelfWriteObj).longValue();
                        if (elapsedSinceSelfWrite &lt; CLIPBOARD_SELF_WRITE_SUPPRESS_MS) return;
                    }

                    cm = (ClipboardManager) context.getSystemService(Context.CLIPBOARD_SERVICE);
                    if (cm == null || !cm.hasPrimaryClip()) return;

                    clip = cm.getPrimaryClip();
                    if (clip == null || clip.getItemCount() == 0) return;

                    // Segunda camada de proteção contra eco: a action "Definir Clipboard" rotula
                    // o ClipData como "reactor", então qualquer clip com esse rótulo é ignorado
                    // mesmo que a janela de tempo acima já tenha passado.
                    clipDesc = clip.getDescription();
                    if (clipDesc != null &amp;&amp; "reactor".equals(String.valueOf(clipDesc.getLabel()))) return;

                    item = clip.getItemAt(0);
                    if (item == null) return;

                    clipText = item.getText();
                    if (clipText == null || clipText.length() == 0) return;

                    text = clipText.toString();
                    hash = Integer.valueOf(text.hashCode());

                    lastHashObj = tasker.getJavaVariable("reactorLastClipboardHash");
                    if (lastHashObj != null &amp;&amp; lastHashObj != void &amp;&amp; lastHashObj.equals(hash)) return;
                    tasker.setJavaVariable("reactorLastClipboardHash", hash);

                    // Origem do app: melhor esforço via último app em primeiro plano conhecido.
                    // O Android não expõe uma API pública confiável para o app de origem de uma
                    // alteração no clipboard, então quando não for possível identificar, o campo
                    // fica vazio em vez de quebrar o monitor.
                    eventContextClip = new HashMap();
                    lastFgPkgObj = tasker.getJavaVariable("reactorLastForegroundPkg");
                    eventContextClip.put("app_package", (lastFgPkgObj != null &amp;&amp; lastFgPkgObj != void) ? (String) lastFgPkgObj : "");
                    eventContextClip.put("text", text);

                    dispatchProviderEvent("clipboard", text, eventContextClip, null, liveCfgClip);
                } catch (Exception clipboardDispatchError) {
                    tasker.log("Erro Reactor monitor de Clipboard: " + clipboardDispatchError.toString());
                }
            }
        };

        clipboardManager.addPrimaryClipChangedListener(clipboardListener);
        tasker.setJavaVariable("reactorClipboardListener", clipboardListener);
    } else {
        tasker.log("Reactor: ClipboardManager indisponível — monitor de Clipboard não será iniciado.");
    }

    cleanupAction = new Action() {
        run() {
            globalDisp = tasker.getJavaVariable("reactorMonitorDisposable");
            if (globalDisp != null &amp;&amp; globalDisp != void) {
                d = (Disposable) globalDisp;
                if (!d.isDisposed()) d.dispose();
            }

            globalClipboardListener = tasker.getJavaVariable("reactorClipboardListener");
            if (globalClipboardListener != null &amp;&amp; globalClipboardListener != void) {
                globalClipboardManager = (ClipboardManager) context.getSystemService(Context.CLIPBOARD_SERVICE);
                if (globalClipboardManager != null) {
                    try {
                        globalClipboardManager.removePrimaryClipChangedListener((ClipboardManager.OnPrimaryClipChangedListener) globalClipboardListener);
                    } catch (Exception ignoredStopListenerError) {
                        // Já não estava mais registrado — seguro ignorar.
                    }
                }
            }

            tasker.setJavaVariable("stopReactorMonitor", null);
            tasker.setJavaVariable("reactorMonitorDisposable", null);
            tasker.setJavaVariable("reactorLastFieldWriteTs", null);
            tasker.setJavaVariable("reactorLastAppPkg", null);
            tasker.setJavaVariable("reactorLastAppTs", null);
            tasker.setJavaVariable("reactorLastForegroundPkg", null);
            tasker.setJavaVariable("reactorLastForegroundTs", null);
            tasker.setJavaVariable("reactorClipboardListener", null);
            tasker.setJavaVariable("reactorLastClipboardHash", null);
            tasker.setJavaVariable("reactorLastClipboardWriteTs", null);
        }
    };
    stopSignal.subscribe(cleanupAction);

    return buildResult(true, "Monitor iniciado");
} catch (Throwable e) {
    err = new JSONObject();
    err.put("ok", false);
    err.put("error", "Java Error: " + e.toString());
    return err.toString();
}</Str>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
			<Int sr="arg5" val="3"/>
			<Int sr="arg6" val="0"/>
		</Action>
		<Action sr="act4" ve="7">
			<code>43</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%action</lhs>
					<op>2</op>
					<rhs>save_config</rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act5" ve="7">
			<code>547</code>
			<Str sr="arg0" ve="3">%java_code</Str>
			<Str sr="arg1" ve="3">import org.json.JSONObject;
import java.io.File;
import java.io.FileWriter;
import com.joaomgcd.taskerm.action.java.JavaCodeException;

try {

    CONFIG_PATH = "/storage/emulated/0/Tasker/Reactor/reactor.json";

    payload = new JSONObject(tasker.getVariable("config"));
    if (!payload.has("config")) throw new JavaCodeException("Config ausente no payload.");
    cfgRoot = payload.getJSONObject("config");

    file = new File(CONFIG_PATH);
    file.getParentFile().mkdirs();
    writer = null;
    try {
        writer = new FileWriter(file, false);
        writer.write(cfgRoot.toString(2));
    } finally {
        if (writer != null) writer.close();
    }

    tasker.setJavaVariable("reactorConfig", cfgRoot);

    monStatus = tasker.getJavaVariable("stopReactorMonitor");
    monitorActive = (monStatus != null &amp;&amp; monStatus != void);

    result = new JSONObject();
    result.put("ok", true);
    result.put("monitorActive", monitorActive);
    result.put("message", "Configuração salva");
    return result.toString();

} catch (Throwable e) {
    err = new JSONObject();
    err.put("ok", false);
    err.put("error", "Erro ao salvar configuração: " + e.toString());
    return err.toString();
}</Str>
			<Int sr="arg2" val="0"/>
			<Int sr="arg3" val="0"/>
			<Int sr="arg4" val="0"/>
			<Int sr="arg5" val="3"/>
			<Int sr="arg6" val="0"/>
		</Action>
		<Action sr="act6" ve="7">
			<code>38</code>
		</Action>
		<Action sr="act7" ve="7">
			<code>37</code>
			<ConditionList sr="if">
				<Condition sr="c0" ve="3">
					<lhs>%java_code</lhs>
					<op>12</op>
					<rhs></rhs>
				</Condition>
			</ConditionList>
		</Action>
		<Action sr="act8" ve="7">
			<code>474</code>
			<se>false</se>
			<Str sr="arg0" ve="3">%java_code</Str>
			<Str sr="arg1" ve="3">%jc_result</Str>
			<Int sr="arg2" val="0"/>
		</Action>
		<Action sr="act9" ve="7">
			<code>43</code>
		</Action>
		<ProfileVariable sr="pv0">
			<clearout>false</clearout>
			<exportval>0</exportval>
			<immutable>false</immutable>
			<pvci>false</pvci>
			<pvd></pvd>
			<pvdn></pvdn>
			<pvid>2018</pvid>
			<pvit>t</pvit>
			<pvn>%count</pvn>
			<pvt>n</pvt>
			<pvv>0</pvv>
			<strout>false</strout>
		</ProfileVariable>
	</Task>
</TaskerData>
